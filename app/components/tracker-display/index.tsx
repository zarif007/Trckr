// Re-export the inline version as the main TrackerDisplay
// The dialog preview has been removed - trackers now render directly in the UI
export { TrackerDisplayInline as TrackerDisplay } from './tracker-display-inline'

