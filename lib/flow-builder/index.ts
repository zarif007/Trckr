/**
 * Reusable flow builder layout and types.
 * Used by ExprFlowBuilder (validation/calculation expressions) and
 * DynamicFunctionFlowBuilder (dynamic options pipeline) to keep the same
 * visual and interaction pattern as field-validation / field-calculation.
 */

export { FlowBuilderLayout, type FlowBuilderLayoutProps } from './FlowBuilderLayout'
